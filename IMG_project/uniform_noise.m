function out = uniform_noise(img, a, b)
    % a: ?????? ??????? b: ?????? ??????
    img = double(img);
    [rows, cols, ch] = size(img);
    
    % ????? ???? ????? ?? ?????? [a, b]
    noise = a + (b - a) * rand(rows, cols, ch);
    
    out = img + noise;
    out = uint8(max(0, min(255, out))); % ???? ????? ???? ????? ?? ???? ??????
end


